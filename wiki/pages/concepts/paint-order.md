---
title: Порядок отрисовки
type: concept
tags: [css, stacking, figma, paint-order]
created: 2026-09-20
updated: 2026-09-20
sources: [packages/serializer/src/stacking.ts, fixtures/stacking/index.html]
---

# Порядок отрисовки

Самая частая причина «импортировалось, но всё перекрыто не тем» — и единственный алгоритм проекта, который пришлось исправлять трижды. Тема заслужила отдельную страницу именно поэтому.

## Почему это вообще трудно

Figma задаёт z-порядок **порядком среди сиблингов**. CSS задаёт его stacking contexts и `z-index`. Это не разные записи одного и того же: CSS умеет выражать порядки, которые дерево Figma выразить не может.

Поле `paintOrder` в [[ir-bundle]] существует ровно для переноса этого знания, а инвариант плотности проверяет его форму: по каждому экрану значения обязаны образовать плотную уникальную перестановку `0..n-1`.

## Правило, которое ломается чаще всего

**Позиционированный элемент участвует в стекинге ближайшего предка-КОНТЕКСТА, а не своего родителя.** Обычная потоковая обёртка контекста не создаёт, поэтому потомок сквозь неё «поднимается».

Классический отказ:

```
<div class="wrapper"><div style="position:relative;z-index:5">P</div></div>
<div style="position:relative;z-index:3">B</div>
```

Без подъёма получается `wrapper, P, B` — выпадающее меню уезжает под соседа обёртки. Правильно `wrapper, B, P`.

Подъём реализован тем, что потоковый ребёнок кладётся в бакет **и обход продолжается внутрь него, собирая в тот же набор бакетов**. Узел, который создаёт контекст или участвует в стекинге сам, кладётся в свой бакет, и обход внутрь не идёт — его поддерево красится вместе с ним.

## Дефект, внесённый исправлением первого дефекта

Различение «атомарная единица» против «потоковая обёртка» вычисляется при сборе и **теряется** к моменту покраски: остаётся только ярлык бакета. А непозиционированный stacking context — созданный `opacity < 1`, `transform`, `filter`, `mix-blend-mode`, `isolation` — попадает в бакет `flow`, потому что признаки «позиционирован» и «z-index значим» для него ложны.

Красильщик потока внутрь не заходил, сборщик тоже. `<div style="opacity:.5">` с содержимым терял **всё** поддерево. Признак восстанавливается в момент покраски отдельной функцией; размещение в бакете `flow` при этом верно по CSS 2.1 Appendix E — непозиционированный контекст красится атомарно на своём месте в потоке.

## Переплетение, которое Figma не выразит

После починки подъёма диапазон поддерева может быть **разорван** чужим узлом. Это и есть невыразимый в Figma случай.

Обнаруживается проверкой **непрерывности**: если поддерево занимает диапазон шире числа своих узлов, внутрь вклинился чужой. Попарное сравнение сиблингов на пересечение здесь не годится — подъём даёт вложенность, а не пересечение.

Знание записывается диагностикой `fidelity.paint-order-interleaved` при захвате. Вычислять его заново ни рендереру, ни плагину не нужно: продюсер уже записал.

## Известный незакрытый случай: pseudo stacking context

`position: relative` (или `absolute`) с `z-index: auto` контекста **не создаёт**, но `isStackingParticipant` трактует такой узел как атомарную единицу, и потомки из него не поднимаются. В коде это зафиксировано как сознательное упрощение с обещанием «вместо моделирования вызывающий обязан породить Diagnostic» — **диагностика не реализована**, и `findInterleaved` её не заменяет: неверный порядок получается внутренне непрерывным, разрыва нет.

Проверено в настоящем Chrome скриншотами, а не по спеке. Обёртка с `z-index: -1`-потомком, перекрывающим потокового соседа обёртки:

| Обёртка | Потомок `z-index:-1` всплывает? | Сериализатор |
|---|---|---|
| `position: static` | нет, уходит под соседа | верно |
| `position: relative; z-index: auto` | нет, уходит под соседа | **неверно: считает атомарной** |
| `position: relative; z-index: 0` | да, остаётся сверху | верно |
| `transform` | да, остаётся сверху | верно |

На `fixtures/stacking` это даёт `.under` с индексом 3 вместо 1: он оказывается после потокового `.flow` и после самой `.stage`. Взаимный порядок остальных узлов верен, а видимых пикселей расхождение в этой фикстуре не меняет — `.stage` прозрачна, а `.flow` с `.under` не перекрываются. Значит **pixel-diff это не поймает**, и снапшот IR сейчас фиксирует слегка неверный порядок. Требует решения: либо моделировать pseudo stacking context, либо выдать обещанную диагностику.

## Мелочь, которая кусает

`getComputedStyle` возвращает указанный `z-index` и для `position: static`. Случайный копипастный `z-index: 0` на статике не должен поднимать элемент над потоковыми соседями, поэтому `z-index` учитывается только у позиционированных и у детей flex/grid.

## Урок про проверки

Тест «все индексы уникальны», написанный как страховка плотности, **не мог** поймать потерю поддеревьев: пропавший узел делает оставшиеся индексы тривиально уникальными, и тест прошёл бы на пустой карте. Теперь он сверяется с числом узлов дерева. См. [[correctness-strategy]].

Тот же дефект воспроизвёлся **второй раз** — уже в e2e-версии теста на фикстуре. Искомый «узел с отрицательным z-index» отбирался предикатом `w === 140 && h === 140 && order > 0`, которому в фикстуре удовлетворяют четыре блока: тест находил `.top` и зеленел, ничего не проверив. Мораль: предикат, отбирающий узел по неуникальному признаку, — то же украшение, что и проверка уникальности на пустом множестве.

## См. также

- [[ir-bundle]] — где живёт `paintOrder` и какой инвариант его проверяет
- [[correctness-strategy]] — правило «проверяй проверки»
- [[fixture-suite]] — фикстуры и снапшоты, на которых это ловится
